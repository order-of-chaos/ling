# Custom Storage Example

This example shows a storage adapter that persists the selected language in the URL query string and implements `subscribe()` so external changes propagate back into `I18nProvider`.
