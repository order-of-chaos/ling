# Core-Only Example

Use `@orderofchaos/ling-core` when you want Ling's translator, storage adapters, and pluralization helpers without React.

See [`example.ts`](./example.ts) for a minimal in-memory setup.
