# ESLint Flat Config Example

This example shows the minimum ESLint 9 flat config for Ling.

Run ESLint against your project and the rule will reject non-literal translation keys, which keeps `ling-scan` reliable.
