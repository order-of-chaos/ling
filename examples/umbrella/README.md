# Umbrella Package Example

Use `@orderofchaos/ling` when you want one install that exposes:

- React runtime APIs
- core runtime utilities
- CLI commands
- ESLint plugin entrypoints

This example mirrors a minimal React setup with the umbrella package.
